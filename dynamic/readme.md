This folder contains dynamic config for Traefik.

Traefik hot reloads these files. No restart necessary.

We have split up files for each type of config to maintain scalability of configs. Traefik combines all files internally.

https://doc.traefik.io/traefik/reference/install-configuration/providers/others/file/ 